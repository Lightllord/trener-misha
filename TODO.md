# TODO

## insight-app → backend push

Сейчас бэкенд ходит в insight-app за данными (GET /draft, GET /state).
Надо наоборот: insight-app сам пушит данные в бэкенд когда считает что есть что отправить.
Убирает polling-паттерн из бэкенда, делает поток данных однонаправленным.

## Draft check на входящих событиях

После реализации push: бэкенд запускает проверку драфтов не на `turn_done`,
а в момент когда приходят события от insight-app.
Delivery (inject в разговор) остаётся на `turn_done` — меняется только триггер для запуска анализа.
Разделение: pending формируется по событиям, доставляется по паузам в диалоге.
