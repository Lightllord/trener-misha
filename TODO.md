# TODO

## Параллельная шина `tryDeliver` / `gameEventQueue` — разобраться

Инсайты теперь едут одним lane через `DeliveryWindow` (см. `backend/README.md`). А игровые события (`gameEventQueue`) + fallback-status по-прежнему ездят отдельной шиной: `tryDeliver()` на `turn_done` + `setInterval(5s)` + `startFallbackTimer(2min)`, гейт только `deliveryWindow.isResponseActive()`. Надо решить, что с этой шиной делать.

Открытые вопросы:

- **Нужна ли вообще параллельная шина?** Или события свести в тот же insight-lane (через `addInsight` → пикер/окно), а `tryDeliver` убрать?
- **Работает ли `gameEventQueue` сейчас?** Проверить, что диффы реально доезжают: может это какая-то полумертвая копия того что есть в другом месте, типа не упустили ли мы события в обычном режиме `tryDeliverInsight` — позиционные инсайты идут через `addInsight`, а диффовые события (`takeEvents`) только через `tryDeliver`. Убедиться, что обе ветки живые и ничего не потерялось при переходе на один lane.
- **Грузить ли что-то в контекст «всегда», без триггера ответа?** Сейчас это делает только `takeFallbackStatus()` (единственный `triggerResponse: false`, снапшот состояния раз в 2 мин). Подозреваем, что это дублирует тулзу `get_match_state`, и засоряет контекст — решить, нужен ли молчаливый пуш данных в принципе и в каком виде; если нет — выпилить `takeFallbackStatus` + `startFallbackTimer`. Может это и не плохо, но может необязательно в таком виде делать. можно раз в какой-то время форсированно прикрпелять блок со служебной гейм информацией к инсайтам наверх - по идеи хороошая практика. 
