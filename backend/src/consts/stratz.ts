/** Purchase-rate % below which an item is flagged as rarely bought on this
 * hero — anchored to boots purchase count as a proxy for "games sampled"
 * (boots are bought in effectively 100% of matches). */
export const RARE_PURCHASE_THRESHOLD_PCT = 5;
