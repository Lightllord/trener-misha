# trener-misha

Before doing anything, read [AGENTS.md](AGENTS.md) — it is the source of truth for this project.
If needed, also read [README.md](README.md) for project overview.

## Commands

Each subproject runs independently from its own directory:

```bash
# Backend
cd backend && npm run dev

# Frontend
cd frontend && npm run dev

# Insight App
cd insight-app && npm run dev
```
